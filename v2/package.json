{
  "dependencies": {
    "url": "^0.11.0"
  },
  "scripts": {
    "start": "npm run --prefix ../client start",
    "build": "CI=false && npm install --prefix client && npm install --prefix server"
  }
}
